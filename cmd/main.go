// Command truebearing is the TrueBearing CLI — a transparent MCP proxy with
// sequence-aware behavioral policy enforcement.
//
// All subcommands are registered in their respective cmd/ subdirectories.
// This file is the entry point only; see cmd/ siblings for command implementations.
package main

import "fmt"

func main() {
	// TODO(1.2): replace with cobra root command execution.
	fmt.Println("[truebearing] scaffold only — run `truebearing --help` after Task 1.2 is complete")
}
